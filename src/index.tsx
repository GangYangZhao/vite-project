import React from 'react';
import ReactDOM from 'react-dom/client';
import { add } from './utils';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>1111</React.StrictMode>
);
